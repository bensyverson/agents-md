#!/usr/bin/env bash
# This repo consumes its own modules: a commit that leaves its AGENTS.md stale
# is refused, exactly as in every other managed repo.
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
agents check || exit 1
